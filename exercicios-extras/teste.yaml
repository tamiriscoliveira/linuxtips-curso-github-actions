name: Teste aula

on:
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: lista arquivo
        run: ls -la ./scripts

      - name: adiciona ṕermissão de execução
        run: chmod +x ./scripts/teste-aula.sh

      - name: Executa o scrip
        run: ./scripts/teste-aula.sh